pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo "hello world"
            }
        }
        stage('Build') {
            steps {
                mvn clean test
            }
        }


    }
}
